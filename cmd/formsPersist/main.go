package main

// launch pages
// GET /input   -> form with inputs
// POST /action -> receives  and validates form inputs. If fail then store values in cookie
//  	MAYBE: go through all tests to collect what is wrong.

// BONUS: when redirect back to input, have it tell user what field is wrong.

func main() {

}
